# AGENTS.md

## 项目定位

Hyperpowers 是一组以 Superpowers 精简重构为基础、并允许独立扩展的 Agent Skills。维护项目时，应保持用户显式控制、技能低耦合和流程可独立结束，不要重新引入已经移除的自动衔接与固定仪式。新增能力不必存在于原版，但应符合本项目的精简定位，并作为清晰、独立的扩展引入。

面向用户的功能契约以 `README.md` 为准，完整设计与技能关系见 `docs/wiki.md`，具体行为由各 Skill 的 `SKILL.md` 及其直接引用文件定义。

## 核心设计约束

- Skill 只接受明确调用：用户直接点名 Skill，或明确选择写有 Skill 名称的选项。
- 任务匹配、文档提及、功能询问、否定表达和模糊的“继续”都不构成调用。
- Skill 可以建议另一个 Skill，但不能静默调用、强制依赖或自动进入后续流程。
- 每个流程都应保留明确的结束方式；完成当前产物不等于自动启动下一个 Skill。
- 保持精简。只为已经观察到的行为问题增加约束，不为现代 Agent 通常能够可靠完成的常规行为堆叠说明。

## 修改 Skill

- 把 Skill 文本视为影响 Agent 行为的程序，而不是普通说明文案。
- 没有具体行为目标和验证依据时，不要仅为调整风格、格式或“符合规范”而重写内容。
- 修改必须基于实际需求、可观察的问题或明确的设计决定；不要虚构失败场景、功能或验证结果。
- 行为契约发生变化时，检查 frontmatter description、Skill 正文、交叉引用、`README.md` 和 `docs/wiki.md` 是否一致。
- 修改激活规则时，应覆盖肯定调用和不应调用的场景；修改其他行为时，应使用相关的 fresh-context 场景验证。
- 如果没有进行 fresh-context 行为验证，应准确说明这一点，不能把文案检查称为行为验证。
